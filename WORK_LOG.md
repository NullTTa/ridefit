# WORK LOG — home-rebuild

집에서 재작업한 내용 정리. 돌아와서 이 파일부터 읽으면 됨.

---

## 추가 수정: images 폴더 전체 재대조 (같은 세션)

사용자가 "그 폴더 안에 있는 것들만 최신화 시켜"라고 요청 → `images` 폴더 전체(34개 파일)를
이름순으로 다시 전수 조사하고, 배포된 파일들과 픽셀 크기/모드(RGB·RGBA)/바이트 수까지 전부
대조함.

**결과**:
- 부품 이미지 11개(지난 세션 교체분), N_MAX 9개(main+360×8), 로고 1개, 신규 차량 5개
  (Address/Burgman/Cygnus/Tricity/Vino): **전부 이미 원본과 바이트 단위까지 완전 일치** —
  추가로 손댈 게 없었음.
- **Super Cub 110의 360도 프레임 8장이 원본보다 훨씬 저해상도(960×451, 예전 리사이즈본)로
  배포되어 있던 것을 새로 발견** — 원본(`images/super_cub/super-cub-110-01~08.png`, 1497×704)
  과 실제로 같은 사진인지 육안 대조까지 마친 뒤 8장 전부 고해상도 원본으로 교체. 360 뷰어
  드래그/프레임전환/FitRoom overlay(머플러) 전부 정상 재확인, 깨진 이미지·콘솔·네트워크
  오류 0건, 모바일 375px 가로스크롤 없음.
- `kitaco-rear-carrier.png`(부품 "KITACO 캐리어")만 여전히 대응 원본이 폴더에 없어서 그대로
  둠(예전 세션에 KITACO 공식 사이트에서 가져온 별도 출처 사진이라 이 폴더 대상이 아님).
- Super Cub 대표 이미지(`super-cub-110.png`, 1829×860)의 원본 출처였던 `suberCub01.jpg/png`
  와 `pcx/` 폴더는 이번 조사 시점에 이미 폴더에서 사라져 있었음(정리하신 것으로 보임) — 둘 다
  이미 프로젝트에 반영 완료된 상태라 문제없음.

수정 파일: `frontend/public/assets/vehicles/super-cub-110/360/01~08.png`.

---

## 추가 수정: PCX 이미지 흰 배경 버그 (같은 세션, 사용자가 직접 발견해서 알려줌)

사용자가 "pcx-main.png를 새로 만들었는데 왜 안 바꿨냐"고 질문 → 조사해보니 **배포된 PCX
main.png + 360 프레임 8장 전부가 유일하게 불투명 흰 배경(RGB)**이었음. 다른 모든 차량
(Super Cub/NMAX/Address/Burgman/Tricity/Vino/Cygnus)은 전부 투명 배경(RGBA)인데 PCX만
예외였고, 그 결과 FitRoom/부품찾아보기 화면에서 PCX만 스테이지 전체가 흰 사각형으로
떠 보이는 실제 버그가 있었음(스크린샷으로 직접 확인). 원본 폴더 `images/pcx/pcx-main.png`
+ `pcx-01~08.png`(전부 RGBA, 같은 해상도 750x300)로 교체해서 해결. 이미지 크기가 동일해
FitRoom 좌표는 안 건드림(26개 배지 전부 경계 내 재확인). 기존 7개 차량 + Cygnus 전부
깨진 이미지 0건, 375px 모바일 가로스크롤 없음, 콘솔/네트워크 오류 0건 재확인.

**교훈**: 지난 세션에 "12개 부품 이미지 최신본 교체"만 하고 차량 대표/360 이미지는
"이미 정상 연결"이라고 판단해 파일 크기/모드 비교를 안 했었는데, 이번에 사용자가 직접
지적해줘서 발견됨 — 앞으로 원본 폴더 대조는 부품뿐 아니라 차량 이미지도 항상 같이 확인할 것.

수정 파일: `frontend/public/assets/vehicles/pcx/main.png`, `frontend/public/assets/vehicles/pcx/360/01~08.png`.

---

## 이전 세션: Burgman 이미지 오배정 해결 + Cygnus Gryphus 125 신규 차량 추가 (2026-09-23 5차, main 위, 커밋 안 함)

지난 세션에서 발견한 "Burgman Street 125 자리에 실은 Yamaha Cygnus Gryphus 사진이 연결돼
있었다" 문제를 정리. 사용자가 그 사이 실제 사진 2장을 `images/`에 새로 준비해둠
(`Burgman Street 125/Burgman Street 125.png` = 진짜 Suzuki Burgman, `Cygnus Gryphus 125/
Cygnus Gryphus 125.png` = 기존에 잘못 쓰이던 그 사진).

1. **Burgman(vehicle_id=6) 이미지 교체** — `frontend/public/assets/vehicles/burgman-street-125/
   main.png`을 진짜 Burgman 사진으로 교체(차체에 "BURGMAN STREET" 인쇄 확인). DB image_url
   경로는 그대로(파일 내용만 교체).
2. **Cygnus Gryphus 125 신규 차량 추가** — 기존 차량 생성 방식을 그대로 따름
   (`resources/seed/vehicle-profiles.json`에 `newModel:{type,years}` 항목 추가 → NMAX/XMAX/
   CB125R과 동일한 시더 경로). Yamaha, type="스쿠터"(플랫플로어 125cc라는 실제 리뷰 표현
   근거로 맥시스쿠터 아닌 일반 스쿠터로 분류), 연식 2021(검색으로 확인한 실제 출시년도,
   chassis_code는 확인 불가라 null 유지). vehicle_model id=13 신규 생성(기존 12개 무변경).
3. **호환성은 최소한만** — "야마하 범용 핸들바 그립 세트"/"야마하 범용 사이드미러 세트"
   (기존 NMAX/XMAX와 완전히 같은 논리 - 이름 그대로 "야마하 범용"이라 Yamaha 신규 모델에
   재사용 가능) 2건만 연결(`ContentSeeder.seedExpansionVehicleParts()`에 독립 블록 추가).
   차종 전용 부품은 만들지 않음. compatibility 594→596건(+2건만).
4. **FitRoom 좌표** — Cygnus 사진(1537×1023)에 100px 격자를 새로 입혀 실측, 호환성이 있는
   미러/핸들바 2개 카테고리만 anchor 등록(기존 Burgman 좌표를 복사하지 않고 독립 실측).
   **부수적으로 발견**: Burgman 이미지가 이번에 실제 사진으로 바뀌면서, 기존에 등록해둔
   Burgman 좌표(직전 세션에 옛 Cygnus 사진 기준으로 측정한 값)가 새 사진과 완전히 어긋나는
   걸 실제로 확인함(머플러 배지가 앞바퀴 쪽에 뜨는 등) → 이건 이번 이미지 교체가 직접 유발한
   회귀라 판단해 새 Burgman 사진(1678×937)도 100px 격자로 재실측해 좌표 10개 전부 교체.
5. **검증**: 로그인 후 차량목록/Cygnus 상세/Cygnus FitRoom(호환부품 2개 체크, 배지 위치·
   충돌 없음, fitSelectionCount 실제 증가), 기존 7개 차량(Cub/PCX/NMAX/Address/Burgman/
   Tricity/Vino) 전부 깨진 이미지 0건, Burgman FitRoom 새 좌표로 20개 부품 전부 경계 내
   정상 배치 재확인, 375px 모바일 가로스크롤 없음, 콘솔/네트워크 오류 0건. DB 무결성(고아
   compatibility 0, 중복 0)과 ModelYear id1~4/MORIWAKI(part id=1) 무변경도 재확인.

수정 파일: `vehicle-profiles.json`, `ContentSeeder.java`, `vehicleFitPositions.js`,
이미지 2장 교체/추가. DB는 시더를 통한 정상 경로로만 변경(직접 SQL 실행 없음). Git 작업 없음.

---

## 이전 세션: P1 - FitRoom 신규 4개 차량 좌표 보정 (2026-09-23 4차, main 위, 커밋 안 함)

Address 125/Burgman Street 125/Tricity 125/Vino 125의 FitRoom 배지가 옛 임시아이콘 기준
범용 0~100% 좌표를 쓰던 문제를 고쳤다. `frontend/src/constants/vehicleFitPositions.js`의
`VEHICLE_FIT_LAYOUTS`에 4개 차종 항목을 신규 추가(기존 Cub/PCX/NMAX 항목은 그대로 유지).

**방법**: 각 차종 `main.png`에 Python(PIL)으로 100px 격자를 입힌 이미지를 만들어 실측 → 실제
호환된 부품 카테고리(머플러/캐리어/미러/시트/스크린/핸들바/램프/휠/프론트바구니/스마트폰거치대
/USB충전기/핸들바가방/배달통/리어백)만 anchor 좌표 등록. 오버레이 이미지는 이 4종엔 없어서
`overlays: {}`로 두고 배지(핀+라벨)만 표시. 사진에서 위치를 확신할 수 없는 항목(보호대=전신
커버라 한 지점 특정 불가, Address의 스크린=실제 사진이 윈드스크린 없는 네이키드 헤드라이트라
부착 지점 확인 불가)은 좌표를 만들지 않고 기존 범용 배지로 남겨둠.

**검증**: 테스트 계정으로 4개 차종을 실제로 내 차고에 등록(최신 연식)한 뒤 FitRoom에서 부품을
여러 개 동시 체크해 배지 위치를 실측 확인 — 전부 스테이지 밖으로 나가지 않고 실제 부품 위치에
자연스럽게 표시됨(예: Tricity 캐리어 배지가 실제 프론트 캐리어 위치, Vino 핸들바 배지가 실제
그립 위치). PartConflict 빨간 강조도 정상 작동(Address 휠+머플러, Tricity 캐리어+스크린).
1440px/375px 양쪽 확인, 콘솔/네트워크 오류 0건. 기존 Cub/PCX/NMAX 세 차종은 좌표를 건드리지
않았고 overlay(Cub 머플러/캐리어)도 그대로 정상 동작 재확인함.

**부수 발견(수정 안 함, 이번 작업 범위 밖)**: Burgman Street 125의 `main.png`를 격자 이미지로
자세히 보니 차체에 "CYGNUS GRYPHUS" 배지가 박혀있음 — 이건 스즈키 Burgman Street가 아니라
**야마하 Cygnus Gryphus 사진**으로 보임(차종 이미지 오배정 의심). 좌표 작업 범위가 아니라
이미지는 교체하지 않았고, 좌표는 이 사진 기준으로 실측해 등록함. 사용자 확인 후 맞는 사진으로
교체 필요.

수정 파일: `vehicleFitPositions.js` 1개. DB 변경 없음, Git 작업 없음.

---

## 이전 세션: 발표 시연 대비 P0 완성도 수정 (2026-09-23 3차, main 위, 커밋 안 함)

전체 정밀진단(발표/시연 관점) 결과 나온 P0 2건만 수정. 새 기능 추가 없음, ModelYear
레거시 4건(id1~4)·MORIWAKI(part id=1) 전혀 건드리지 않음.

1. **P0-1: 예약/마이페이지 빈 서비스정보 표시 수정** — 정비예약이 1건당 서비스 1개만 담던
   구조에서 1:N(ReservationItem)으로 리팩터링되기 전 만들어진 예약 5건(RF-00002~00005)이
   `items`가 빈 배열이라 "· · 날짜"처럼 빈칸으로 보이던 문제. `Reservations.jsx`/`MyPage.jsx`
   에서 `items.length===0`일 때만 "서비스 정보 없음"으로 자연스럽게 표시하도록 조건부 렌더링
   추가(가격/서비스명을 지어내지 않음, DB는 손대지 않음, 정상 예약 RF-00006은 그대로 표시).
2. **P0-2: 마이페이지 "최근 확인한 부품" 중복 제거** — 같은 부품을 여러 번 보면 로그가 쌓여
   그대로 목록에 여러 번 뜨던 문제. `MeController.getRecentChecks()`를 30건 넉넉히 가져온 뒤
   `partId` 기준으로 가장 최근 것만 남기고(LinkedHashMap) 최대 10건 반환하도록 수정. DB 로그
   자체는 삭제하지 않음(조회 시점 dedup만).
3. **전체 회귀 확인** — 로그인→차량상세(Cub, 360 8프레임 정상)→부품찾아보기→부품상세→FitRoom
   (overlay 이미지 정상 반영)→예약→마이페이지까지 재확인. `getVehicleStageAspectRatio()`,
   `fitSelectionCount`, `PartPopularityService`, 360 Viewer, 이전 세션에 고친 모바일
   가로스크롤(375/390/768) 전부 회귀 없음 확인. console error 0건, network 4xx/5xx 0건.

수정 파일: `MeController.java`, `Reservations.jsx`, `MyPage.jsx`. Git 작업 없음(commit/push
전혀 안 함).

---

## 이전 세션: 전체 QA(데이터/흐름/UI) 검수 (2026-09-23 2차, main 위, 커밋 안 함)

"새 기능 말고 전체 검수" 지시로 진행. 조사 → 문제 발견 → 최소 수정 → 재검사 순서로 진행.
실제로 수정한 버그 2건, 실제 데이터 조사 결과는 대화 내 최종 보고서 참고(요약만 기록).

### 실제로 고친 문제
1. **React 중복 key 경고(실제 원인 추적함)** — `VehicleDetail.jsx`의 호환부품 목록에서
   `key={y.year}`를 쓰고 있었는데, Super Cub 110/PCX의 `model_year` 테이블에 `model_year_value`
   가 NULL인 레거시 로우가 차종당 2개씩 남아있어서(Cub: id1 JA71/id2 JA07, PCX: id3 KF30/id4
   KF47 — 전부 연식 값이 NULL) 한 부품이 이 두 레거시 연식 모두에 호환 등록돼 있으면 `key=null`
   이 두 번 나와 React가 경고를 냈음. `key={`${y.year ?? 'na'}-${y.status}-${i}`}`로 수정.
   (레거시 ModelYear 로우 자체는 삭제 안 함 — 아래 "발견했지만 안 고친 문제" 참고)
2. **모바일 가로 스크롤 버그** — 같은 화면(`VehicleDetail.jsx` 호환부품 목록)에서 연식이 6개인
   차량(Cub/PCX)은 "2010 호환가능 2015 호환가능 ..." 배지가 `shrink-0`+줄바꿈 없이 한 줄로
   강제되면서 375/390/768px에서 가로 스크롤이 발생했음(1024px 이상에서만 안 보여서 지금까지
   못 찾았던 버그). 배지 컨테이너를 `flex flex-wrap`으로 바꿔서 해결, 375/390/768 재확인 완료.

### 부품 데이터 확충 (사용자 승인 받고 진행)
`ContentSeeder.java`에 단일상품 카테고리 13종 +1개, 스마트폰거치대/USB충전기 +2개(총 신규
15개 Part) 추가 + 오분류 카테고리 2건("네이키드 라운드 미러 세트" 사이드미러→미러, "숏 스포츠
스크린 (PCX)" 윈드스크린→스크린) 자동 정정 로직 추가. 전체 부품 71→86개.

### 이미지 실제 연결 (사용자 승인 받고 진행)
- Address 125/Burgman Street 125/Tricity 125/Vino 125 대표이미지를 관리자 API로 연결
  (`frontend/public/assets/vehicles/{slug}/main.png` 신규 생성 후 DB 반영). 360은 원본 자체가
  없어서 추가 안 함.
- 부품 이미지 11개를 9/22 최신 원본으로 교체(파일명 유지, 내용만 교체).
- OSAKA 머플러(id13)가 순정 스테인리스(id32)와 이미지를 공유하던 버그 수정 — 신규 파일
  `osaka-cub110-slipon-exhaust.png` 생성해 분리 연결.

### 발견했지만 안 고친 문제 (사용자 판단 필요)
- **레거시 ModelYear 4건**(Cub: id1 JA71-연식NULL, id2 JA07-연식NULL / PCX: id3 KF30-연식NULL,
  id4 KF47-연식NULL) — 이후 세션에서 JA07/JA10/JA44(Cub), JF28/JF56/JF81(PCX)로 정확한 연식을
  다시 시드했지만 이 구버전 4개 로우가 삭제 안 되고 남아 compatibility에도 걸려있음. DB 삭제는
  이번에도 안 함(지시대로) — 삭제 후보/영향은 최종 보고서 참고.
- **부품 상세에 "호환 차량 목록" 섹션 자체가 없음** — 현재 API(`/api/parts/{id}`)가 호환 차량
  목록을 아예 내려주지 않아서(특정 차량 1개 기준 호환 여부만 체크 가능), 새 API+UI를 만드는
  건 "새 기능 추가 금지" 지시에 걸릴 수 있어 손대지 않고 보고만 함.
- 이미지 없는 부품 70/86개, 판매처 없는 부품 68/86개 — 실제 이미지/판매처 데이터가 없는 것이
  맞아서 그대로 "이미지 준비중"/"등록된 판매처 없음"으로 유지.

### 재검사 결과
Playwright로 로그인 후 TEST1~10(요청 시나리오 그대로) + 부품/차량 상세 다수 페이지를 다시 돌며
console error 0건, network 4xx/5xx 0건, 깨진 이미지 0건 확인. 백엔드 로그도 ERROR/Exception
없음 확인.

---

## 이전 세션: 실측 검증 + 이미지/데이터 반영 (2026-09-23, main 위, 커밋 안 함)

"코드 설명 말고 실제 DB/화면 기준으로 검증해달라"는 지시로 진행. **직전 세션(9/22) 보고 중
일부가 부정확했음이 이번에 드러남** — 아래 항목 확인.

### 검증에서 새로 드러난 사실
- **부품 총 개수 정정**: 9/22 보고에서 "부품 30개"라고 썼는데 이는 오래된 WORK_LOG 문구를
  그대로 믿은 것이었고, 실제 DB `SELECT COUNT(*)`는 **71개**였음(9/23 보강 후 86개). 총량
  숫자를 볼 땐 항상 DB를 직접 세어서 확인할 것 — WORK_LOG 문구를 곧이곧대로 믿지 말 것.
- **부품 이미지가 구버전이었음**: `frontend/public/assets/parts/`의 12개 부품 이미지가 전부
  2026-09-21 배포본이었는데, 사용자가 재작업한 최신 원본(`.../images/` 폴더, 2026-09-22 촬영)
  이 파일 크기까지 다른 채로 방치돼 있었음 → 11개 교체 완료(kitaco-rear-carrier만 대응 원본
  없어서 유지).
- **부품 이미지 재사용 버그**: id13 OSAKA 슬립온 머플러와 id32 순정 스테인리스 머플러가 같은
  `cub110-stainless-exhaust.png`를 쓰고 있었음 → `osaka-cub110-slipon-exhaust.png` 신규 생성해
  id13에 연결, 관리자 API(`/api/admin/parts/13/image`)로 반영, 브라우저에서 서로 다른 사진
  으로 뜨는 것 확인.
- **차량 4종 이미지 미연결**: Address 125 / Burgman Street 125 / Tricity 125 / Vino 125는
  `vehicle_model.image_url`이 NULL이라 파란 선 아이콘 플레이스홀더로 표시되고 있었음(원본 대표
  사진은 폴더에 있었는데 프로젝트에 연결만 안 된 상태). `/api/admin/vehicle-models/{id}/image`
  로 4종 전부 연결, 실사진 표시 확인. 이 4종은 360도 연속사진 자체가 원본에 없어서 360은 추가
  안 함(정지 대표사진만).
- **카테고리 오분류 2건 발견/수정**: "네이키드 라운드 미러 세트"가 category="사이드미러"로
  잘못 들어가 있어서(정상은 "미러") 마치 "미러 카테고리가 1개뿐"인 것처럼 보였음. "숏 스포츠
  스크린 (PCX)"도 category="윈드스크린"으로 잘못 들어가 있었음(정상은 "스크린"). `ContentSeeder`
  에 `recategorize()` 헬퍼 추가해 재기동 시 자동 정정되도록 함(멱등).
- **단일 상품 카테고리 15종 확충**: 사이드백/풋페그/엔진가드/리어쇼크/프론트캐리어/탑박스/
  리어백/핸들바가방/핸드가드/너클가드/프론트바구니(각 1→2개), 스마트폰거치대/USB충전기(각
  1→3개) — `ContentSeeder.seedRiderAccessoryParts()`에 15개 신규 `Part` + 호환성(Compatibility)
  추가. 이미지 없는 신규 항목은 imageUrl을 채우지 않아 "이미지 준비중"으로 정직하게 표시됨
  (가짜 이미지 연결 안 함).
- **MORIWAKI XXX 삭제는 이번에도 실행 안 함** — 사용자가 "삭제는 내가 직접 한다"고 확인.
  FK 참조만 재확인(`recent_part_check` 2건, `compatibility` 1건, 나머지 0건). 삭제 SQL은
  이전 세션 기록 그대로 유효함(아래 "남은 것" 참고).
- 정비 예약 다중선택/오일종류/가격 계산은 실제 클릭해서 재검증 완료(기본점검 20,000+엔진오일
  35,000=55,000 → 반합성유 +5,000=60,000 → 타이어교체 +80,000=140,000, 전부 정확).
  360도 드래그/자동회전, FitRoom 체크→실제 오버레이 이미지 DOM 반영도 실제 클릭 확인.
- Git 작업(커밋/푸시) 전혀 안 함 — 지시대로.

---

## 이전 세션: 전체 UX/UI 완성도 개선 (2026-09-22, main에 직접 커밋+push 완료)

"실제 사용자 관점에서 점검 후 개선" 지시(25개 항목)에 따라 진행. `main` 브랜치에 커밋 7개로
나눠서 작업하고 **push까지 완료**함(`a298f4e..cc67921`). 이후 이어서 작업할 때는 이 목록에서
"남은 것"부터 확인하면 됨.

### 이번 세션에서 완료한 것
1. **정비 예약 다중 선택 + 엔진오일 종류 + 총액 계산** — `Reservation`을 `ReservationItem`
   1:N 구조로 리팩터링(기존엔 서비스 1개만 담을 수 있었음). 체크박스로 여러 서비스 동시 선택,
   "엔진오일 교환" 선택 시 광유/반합성유/합성유(기본가 대비 +0/+5,000/+15,000원, RIDEFIT
   자체 정책값) 선택 가능. 서버가 `ShopMaintenancePrice` 조회해 최종 금액 확정(클라이언트
   값 불신). 로컬 MySQL에 실제로 curl 테스트해서 계산 정확성 + 검증 실패 케이스(다른 서비스에
   오일종류 지정/중복선택/빈선택 전부 400) 확인 완료.
   - 새 파일: `domain/EngineOilType.java`, `domain/ReservationItem.java`
   - 수정: `domain/Reservation.java`, `controller/ReservationController.java`,
     `controller/ServiceShopController.java`(엔진오일 옵션 카탈로그 API 추가),
     `frontend/src/pages/ServiceDetail.jsx`(전면 재작성)
2. **"가상 예약" 표현 제거** — ServiceDetail/Reservations/MyPage/Home/Guide/Services 7곳
   사용자 노출 문구 정리(개발자 주석은 안 건드림).
3. **360도/FitRoom 크기 기준 통일** — 실측(PowerShell로 PNG 픽셀 해상도 확인) 결과 대표사진과
   360프레임 비율이 거의 동일한데도, "부품 장착" 화면과 "360도 보기" 화면이 서로 다른 고정
   비율(4:3 vs 16:10)을 써서 전환 시 차량 크기가 달라 보이던 버그를 찾아 공유 함수
   `getVehicleStageAspectRatio()`(`constants/vehicleFitPositions.js`)로 통일. FitRoom에도
   360도 보기 토글 신규 연동.
4. **부품 카탈로그 확충** — 배달 라이더 중심 신규 카테고리 14종(스마트폰거치대/USB충전기/
   배달통 2종/리어백/핸들바가방/보호대 2종/핸드가드/너클가드/프론트바구니/LED보조등/
   레버프로텍터/열선그립/차종별 에어필터) 추가, `ContentSeeder.seedRiderAccessoryParts()`.
   실제 장착 가능성 기준으로 호환성 연결(캐리어 있는 차종에만 배달통 등).
5. **Header/Footer/Home 톤 보강** — 실제 로고 이미지 적용(CSS "RF" 배지 대체), 활성 메뉴
   포인트, 히어로 그라디언트, 카드 hover 통일. 큰 폭 리디자인은 안 함(아래 "남은 것" 참고).
6. **예전 빈 360 플레이스홀더 폴더 삭제** — `vehicles/360/super-cub-110/`(README만 있던
   빈 폴더, 실제 사진은 `vehicles/super-cub-110/360/`에 있음 — 경로 순서가 뒤바뀐 예전 흔적).

### 남은 것 (다음 세션에서 이어할 것)
- **MORIWAKI XXX 삭제** — 코드/시드 JSON엔 없지만 **로컬 MySQL DB `/parts/1`에 레거시로
  남아있음**(현재 시더가 만든 게 아니라 과거에 DB에 직접 넣은 데이터). 참조 확인 결과
  Compatibility 1건 + RecentPartCheck 2건만 걸려있어 삭제해도 안전함을 확인했으나,
  Claude Code 자동 모드의 "되돌릴 수 없는 삭제" 안전장치가 DB DELETE 실행을 막아서
  사용자가 직접 실행해야 함:
  ```sql
  DELETE FROM recent_part_check WHERE part_id=1;
  DELETE FROM compatibility WHERE part_id=1;
  DELETE FROM part WHERE id=1;
  ```
- **Home 섹션 큰 폭 비주얼 리디자인** — 이번 세션엔 시간 제약으로 hover/그라디언트 등
  안전한 수준의 보강만 함. Hero/카드 레이아웃 자체를 더 과감하게 바꾸는 건 안 건드림.
- **브라우저 실기기/모바일 클릭 테스트** — API curl + `npm run build` + 백엔드 유닛테스트로만
  검증했고, Playwright 등으로 실제 화면을 띄워 확인하지는 못함.
- **신규 부품 14종 실물 이미지** — 아직 없어서 FitRoom에서 "이미지 준비중" 텍스트 배지로만
  표시됨.

---

## 추가 세션: 데이터/UX 완성도 보강 (6개 항목, 자율 진행)

`main` 브랜치에서 그대로 진행(별도 브랜치 없음, **push 안 함**). 커밋 4개로 나눠서 진행:

1. **차량 등록 "사진 URL" 입력 제거** — `VehicleModel.imageUrl`을 추가하고 관리자 전용
   `/admin/vehicle-models`에서 차종별 대표 이미지를 등록/수정하도록 바꿈. 사용자는 제조사/모델/
   연식만 고르면 등록된 이미지가 자동 적용되고, 없으면 기존 임시 아이콘 표시.
2. **Super Cub 110 실제 이미지 적용** — `D:\개인프로젝트\ridefit_images\SuperCub.png`를
   `frontend/public/assets/vehicles/`에 복사. PNG가 RGBA(알파 채널 포함)라 배경 투명 확인,
   다크 테마 위에 자연스럽게 보임. 내 차고 카드/부품 입혀보기 화면에 적용됨.
3. **부품 입혀보기(FitRoom) 화면 신규** — `/garage/:id/fit`. 차량 이미지 위에 호환 부품을
   카테고리별로 토글(장착/해제), 여러 개 동시 On/Off 비교 가능. 기존 PartConflict 체크
   로직을 재사용해 동시 장착 시 충돌 경고 표시. 부품 컷아웃 이미지가 없어서 카테고리 위치에
   텍스트 배지로 오버레이(요청사항에 명시된 대체 방식).
4. **부품 링크 확인 화면 캐스케이드 "버그"** — 실제 클릭 기반으로 반복 재현했지만 로직 자체는
   정상 동작함을 확인(Playwright로 클릭 + elementFromPoint 히트테스트까지 검증). 제조사 선택
   전 모델/연식이 disabled로 흐려지는 게 "반응 없음"처럼 보이기 쉬운 구조라, disabled
   플레이스홀더를 "먼저 제조사를 선택하세요"로 바꿔 원인을 명확히 함.
5. **연식 시드 데이터를 세대/프레임코드 기준으로 재구성** — 검색으로 실제 확인한 값만 사용:
   Super Cub 110(JA07/JA10/JA44), PCX125(JF28/JF56/JF81 — 예전 "KF30"은 다른 배기량 코드라
   틀렸던 값, 바로잡음), Vino125(2004~2009년만 실제 판매 — 예전 2021/2023년식은 존재하지도
   않던 연식이라 2005/2008로 수정), Address125(2021년 풀체인지 기준 구/신형 구분),
   Burgman Street125(국내 2023년 첫 출시 — 이전 연식 삭제하고 2023/2025로 수정), Z125(BR125
   코드 확인). 코드 없는 것(Tricity125/Ninja125/Address125 코드 등)은 지어내지 않고 null 유지.
   세대에 맞춰 Compatibility도 재검토(범용부품 vs 세대전용부품 구분).
6. **관리자 계정 지정 + 헤더 버튼** — 이미 가입된 `sk05ek@naver.com`을 DataSeeder에서
   idempotent하게 ADMIN으로 승격(계정 없으면 아무 것도 안 함). 헤더 프로필 옆에 ADMIN만
   보이는 "관리자" 버튼 추가. 관리자 회원 목록 API로 role이 ADMIN으로 바뀐 것 직접 확인함
   (본인 계정 비밀번호는 몰라서 직접 로그인 테스트는 못 함 — 한 번 로그인해서 버튼이 실제로
   보이는지 확인해주세요).

전부 실제 로컬 MySQL에 붙여서 재실행 + Playwright 스크린샷으로 직접 확인 완료. 스모크
테스트도 재통과.

- 브랜치: `home-rebuild` (이 브랜치에서 전부 작업 후 `main`에 merge, merge까지 commit함. **push는 안 했음** — 직접 확인 후 push할 것)
- 지시사항의 "1~18번 다 만든 뒤 19번 스모크테스트" 순서대로, 그리고 핵심 등급을 우선으로 전부 진행함. 1~20번 **전부 구현 완료**.

## 우선순위 등급 해석에 대해 (먼저 읽어주세요)

지시문의 "우선순위" 문단에 적힌 번호(예: "9번(관리자 CRUD)", "10번(시드 데이터)")가 지시문 본문 자체의 섹션 번호(관리자 CRUD=7번, 시드 데이터=8번)와 어긋나 있었습니다. 번호 대신 **내용(관리자 CRUD, 시드 데이터 등)**을 기준으로 매칭해서, 핵심 등급을 다음으로 해석하고 진행했습니다:

- 핵심: 인증(1), 호환성 검사(3), AI 합성 mock(5), 관리자 CRUD(7), 시드 데이터(8), 스모크 테스트(19), 마무리(20)
- 이 해석이 의도와 다르다면 알려주세요.

결과적으로는 시간이 넉넉해서 20번까지 전부 끝냈기 때문에 이 해석 차이가 실제 결과물에 영향을 주진 않았습니다.

## 무엇을 했는지 (커밋 순서대로)

1. **인증 (JWT)** — Role(USER/ADMIN), Member에 BCrypt 비밀번호/role/email unique. `/api/auth/signup`, `/api/auth/login`. Spring Security + JWT stateless 필터. 차량 등록 등은 클라이언트 memberId가 아니라 JWT의 인증된 사용자로 처리(IDOR 방지). 프론트: 로그인/회원가입 페이지, Authorization 헤더 자동 첨부 API 헬퍼(`lib/api.js`), RequireAuth 가드, 헤더 로그인 상태 UI.
2. **테마** — 다크모드 토글 제거, 블루블랙 단일 테마(`#0B0E14`/`#131824`/`#232B3A`/`#3B82F6`/`#E7ECF3`/`#8B95A8`), 로고만 오렌지(`#FF6B35`) 유지.
3. **호환성 검사 (핵심)** — Part에 `sourceUrl`/`imageUrl` 추가. Jsoup으로 판매 링크 크롤링(제목/가격/이미지, 상품명에서 차종+연식 자동 인식 시도). 크롤링 실패는 정상 상황으로 취급해 수동 입력 폼으로 자연 전환. 자동/수동으로 확정된 차종에 Compatibility 레코드 생성 → "차량 선택 즉시 호환 여부 표시"는 이 레코드 존재 여부로 판정, 호환 안 되면 메시지만 보여주고 다음 단계 차단. 내 차고(Garage), 부품 찾아보기(카테고리 필터), 마이페이지 "최근 확인한 부품" 모두 구현.
4. **부품 충돌 감지** — PartConflict(partA, partB, reason) 최소 구조. 부품 찾아보기에서 여러 부품 체크 후 충돌 확인 가능.
5. **AI 이미지 합성 (핵심)** — `AI_SYNTH_MODE=mock|live`, 기본 mock(고정 SVG 샘플 반환). live 코드는 Pollinations `/v1/images/edits` 호출로 구성해뒀지만 **이번 세션에선 호출 안 함** (지시대로 mock 유지). 서버가 호환성 통과 여부를 다시 검증한 뒤에만 합성 실행. 크롤링+합성 합산 일일 20회 RateLimit. 실패 시 "지금은 이미지를 만들 수 없어요..." 안내로 감쌈.
6. **커뮤니티** — 목록(페이지네이션)/상세(댓글)/글쓰기. 장착 부품 + 호환 여부(맞았어요/안맞았어요) 기록 구조만 마련(Compatibility 자동 반영 로직은 스펙대로 안 만듦).
7. **관리자 CRUD (핵심)** — Compatibility/PartConflict 표 CRUD(둘 다 페이지네이션), 회원 목록(role 조회만, 변경 API는 의도적으로 없음 — ADMIN 전환은 DB 직접 수정 정책), 대시보드 통계 카드(17번도 같이 됨).
8. **시드 데이터 (핵심)** — DataSeeder(CommandLineRunner). Honda Super Cub 110 / PCX, 부품 9개, Compatibility(호환가능/브라켓필요/호환불가 다 포함), PartConflict 2건. 테스트 계정: `user@ridefit.dev` / `user1234!` (USER), `admin@ridefit.dev` / `admin1234!` (ADMIN).
9. **품질 다듬기** — 호환성 판정/PartConflict 필터링 단위 테스트. 실제 MySQL 없이 테스트가 돌아가도록 H2 기반 `test` 프로필 추가. 이 과정에서 `ModelYear.year` 컬럼명이 SQL 예약어와 충돌하던 실버그를 발견해 수정(`model_year_value`로 변경). springdoc-openapi는 이미 추가돼 있어서 별도 설정 없이 `/swagger-ui/index.html` 동작.
11. **Home/로그인/회원가입 애니메이션** — 차량 아이콘 중심 부품 라벨 5개가 8초 주기로 라운드로빈 표시 + 은은한 글로우, 차량 펄스 효과(tailwind keyframes). 로그인/회원가입 좌우 분할, Home은 같은 구조를 풀스크린 히어로로 재사용(비로그인 시 오른쪽에 실제 로그인 폼). 차량 이미지 경로는 `constants/images.js`로 분리해둠 — 나중에 실물 사진으로 교체 시 이 파일만 바꾸면 됨.
12. **화면 다듬기** — 404 페이지 추가. 빈 상태/로딩/유효성 메시지는 각 화면 구현 시점에 함께 처리함(별도 단계로 안 미뤘음).
13. **최저가 비교** — SellerListing 엔티티. "등록된 판매처 중 최저가"이지 실시간 자동 검색이 아님을 UI 문구에도 명시. 기존 크롤링 로직 재사용.
14. **영상 콘텐츠** — 유튜브 링크 임베드(YoutubeEmbed 컴포넌트). Part.installVideoUrl(카테고리별 설치 영상, 관리자가 `/admin/part-videos`에서 등록), Post.videoUrl(게시글 작성자가 등록).
15. **계정 관리 & 편의기능** — 닉네임/비밀번호 변경, 회원 탈퇴(차량/즐겨찾기/기록은 삭제, 게시글/댓글은 작성자 연결만 끊어 익명화 보존). JWT 만료 시 자동 로그아웃 + 로그인 페이지 리다이렉트 + 안내 문구(RequireAuth가 자동 처리). 부품 즐겨찾기(★ 토글, 마이페이지에서 조회/해제). 커뮤니티 글쓰기 사진 첨부(로컬 파일 업로드 → `/uploads/**`).
16. **페이지네이션** — 커뮤니티 목록, 관리자 회원/Compatibility/PartConflict 목록 모두 Spring Data `Page` + 이전/다음 UI로 적용됨 (3번/7번 작업 중 같이 처리).
17. **관리자 대시보드 통계** — 8번에 포함해서 처리함 (회원/차량/부품/게시글 수 카드).
18. **README.md** — 프로젝트 소개/기능/기술스택/로컬 실행법/환경변수 표/스크린샷 자리(플레이스홀더) 작성.
19. **스모크 테스트 (핵심)** — `SmokeTest.java`: 회원가입→로그인→차량 등록→부품 등록(수동 입력 경로)→라이브 호환성 확인→AI 합성(mock)→커뮤니티 글쓰기/댓글→일반 유저 관리자 접근 차단 확인→관리자 로그인→회원/Compatibility 관리 화면 접근→회원 탈퇴까지 MockMvc로 실제 실행. **이 과정에서 실제 버그를 하나 발견해서 고쳤음**: 회원 탈퇴 시 MyVehicle을 먼저 지우면서 그 차량을 참조하는 Post.myVehicle 외래키를 위반해 500 에러가 나고 있었음 → 차량 삭제 전에 관련 게시글의 author/myVehicle 연결을 먼저 끊도록 순서를 고침. 겸사겸사 GlobalExceptionHandler가 예외를 로그도 안 남기고 삼키던 것도 로깅하도록 고침.
20. **마무리** — 이 파일. 아래에 막힌 부분/확인 필요한 부분 정리.

## 막히거나 확인이 필요한 부분

- ~~로컬 MySQL 연결을 실제로 테스트하지 못했음~~ → **[추가 세션에서 해결됨]** 실제 로컬 MySQL(root/1234)로 접속 정보를 받아서 `application-local.properties`에 채우고, JDBC URL에 `createDatabaseIfNotExist=true`를 추가해 `ridefit` 스키마를 앱이 직접 생성하도록 했습니다. `./gradlew bootRun`으로 실제 MySQL에 붙여서 기동 확인, 시드 데이터(제조사/부품 한글 포함) 정상 조회, `user@ridefit.dev` 로그인, Swagger UI(`/swagger-ui/index.html`) 전부 실제로 확인했습니다. `application-local.properties`는 `.gitignore`되어 있어 이 계정 정보는 커밋되지 않았습니다 — 필요하면 직접 다른 계정으로 바꿔도 됩니다.
- ~~브라우저 스크린샷으로 UI를 직접 보지 못했음~~ → **[추가 세션에서 해결됨]** Playwright(headless Chromium)를 별도로 설치해서 로그인/회원가입/홈/부품찾아보기/커뮤니티/마이페이지/관리자 화면을 데스크톱(1280px)과 모바일(390px) 양쪽에서 실제로 렌더링해서 확인했습니다. 이 과정에서 실제 버그를 하나 찾아서 고쳤습니다: `/login`, `/signup` 페이지가 모바일에서는 왼쪽 애니메이션 패널이 숨겨지는데 `min-h-[calc(100vh-4rem)]`는 그대로 적용돼서, 폼이 화면 한가운데 붕 뜨고 위아래로 큰 빈 공간이 생기는 문제였습니다 → `md:` 브레이크포인트부터만 최소 높이를 적용하도록 수정(커밋 `f38ca97`). 그 외 화면들은 스크린샷상 깨진 곳 없음을 확인했습니다.
- **AI_SYNTH_MODE는 계속 mock입니다.** live 전환용 Pollinations 호출 코드(`AiSynthService.callPollinations`)는 작성해뒀지만, 정확한 요청/응답 스키마는 Pollinations 공식 문서를 보고 검증된 게 아니라 합리적으로 추정해서 작성한 것입니다. 실제로 live로 켜기 전에 응답 파싱 부분(`json.path("data").path(0).path("url")`)이 실제 API 응답 구조와 맞는지 한 번 확인해주세요.
- **차량 사진은 여전히 임시 아이콘(`frontend/src/assets/hero.png`)입니다.** `frontend/src/constants/images.js` 파일 하나만 실제 사진으로 바꾸면 Garage 카드/애니메이션 전체에 반영됩니다.
- 관리자 Compatibility/PartConflict 추가 폼은 부품을 드롭다운(이름만)으로 고르게 되어 있어서, 부품이 많아지면 검색이 안 되는 게 불편할 수 있습니다 — 이번 범위에선 기능 동작까지만 맞췄습니다.
- 의도적으로 안 만든 것 (지시사항대로): 3D 부품 미리보기, 라이브 코칭 화상/음성·실시간 채팅.

## 확인해볼 것 (선택)

- `git log main..home-rebuild`로 커밋 15개 확인 가능. 각 커밋이 기능 단위로 나뉘어 있습니다.
- 로컬 실행 방법은 `README.md` 참고.

---

## 추가 세션: 밤사이 데이터/완성도 보강 (자율 진행)

`main` 브랜치 위에서 그대로 진행 (별도 브랜치 없음, 여전히 로컬 상태, **push 안 함**). 목표는 새 기능 추가가 아니라 이미 만든 기능이 실제 데이터로 꽉 차 보이게 하는 것.

### 1~2. 차량 시드 데이터 대폭 확장

제조사 **1개 → 4개**(Honda + Yamaha/Suzuki/Kawasaki 신규), 모델 **2개 → 8개**로 확장. 신규 모델은 전부 검색으로 확인한 실존 모델명만 사용:
- Yamaha: **Tricity 125**(트리시티 125), **Vino 125**(비노 125)
- Suzuki: **Address 125**(어드레스 125), **Burgman Street 125**(버그만 스트리트 125)
- Kawasaki: **Ninja 125**(닌자 125), **Z125** — 두 모델 다 스쿠터가 아니라 125cc 소형 모터사이클이지만, 가와사키는 국내에 125cc 이하 스쿠터 라인업이 없어서(검색으로 확인) 실존하지 않는 가와사키 스쿠터를 지어내는 대신 실제 판매 중인 이 두 모델을 사용함.
- 모델당 연식 2개(2021/2023)씩, 총 ModelYear 4개 → 16개. 신규 모델의 chassisCode는 정확한 값을 검색으로 확인하지 못해서 지어내지 않고 `null`로 비워둠(Honda 기존 두 모델만 실제 프레임 코드 JA44/KF30 유지).

### 2. 부품 & 호환성 데이터 확장 (필수 처리함)

부품 **9개 → 30개**(신규 21개). 머플러/핸들바/미러/시트/휠/스크린/캐리어/램프/레버 카테고리에 걸쳐, "제조사 범용 부품"(예: 야마하 범용 사이드미러 세트 - 트리시티/비노 양쪽 다 호환)과 "모델 전용 부품"(예: 트리시티 프론트 캐리어)을 섞어서 구성. 호환가능/브라켓필요/호환불가 상태도 신규 부품에 고르게 섞어둠(예: 비노 크롬 머플러는 2021년식만 되고 2023년식은 배기 규격 변경으로 호환불가). Compatibility 레코드는 **4건 → 71건**으로 늘어남.

PartConflict **2건 → 7건**(신규 5건): 트리시티 캐리어+스크린 마운트 간섭, 비노 머플러 배기열+휠커버 변형, 어드레스 휠+머플러 클리어런스 부족, 버그만 미러+방향지시등 마운트 겹침, 닌자125 스크린+클립온 핸들바 간섭.

### 3. 최저가 비교 시드

부품 5개(Cub 머플러, PCX 캐리어, 트리시티 캐리어, 어드레스 캐리어, Z125 미러)에 판매처 2~3곳씩(SellerListing 13건) 등록해서, 부품 찾아보기 화면에서 실제로 "최저가" 배지가 보이는 상태로 만듦. 화면에서 직접 확인 완료.

### 4. 커뮤니티 샘플 게시글

게시글 **0개 → 9개**(후기 4 / 호환여부공유 3 / 질문 2 톤 혼합), 댓글 9개. 작성자 다양화를 위해 테스트 회원 4명 추가(`rider_min@ridefit.dev`, `scooter_fan@ridefit.dev`, `commuter_kim@ridefit.dev`, `newbie_park@ridefit.dev`, 비밀번호 전부 `rider1234!`). 일부 글에는 실제 존재하는 유튜브 영상(혼다코리아 PCX 리뷰, 슈퍼커브 머플러 튜닝 영상 등, 검색으로 확인한 실제 URL)을 연결해서 "관련 영상" 섹션이 비어 보이지 않게 함.

### 5. 관리자 대시보드 숫자 확인

확장 후 실제 로컬 MySQL에 붙여서 직접 확인: **회원 7 / 등록 차량 1 / 부품 30 / 게시글 9** — 관리자 대시보드 카드 숫자와 정확히 일치함을 API/화면 양쪽에서 확인.

### 6. 전체 화면 훑어보기 - 추가로 보강한 곳

- **마이페이지**가 즐겨찾기/최근확인 둘 다 비어 있어서 허전했음 → 테스트유저(`user@ridefit.dev`) 기준 즐겨찾기 3건, 최근 확인 기록 3건(등록된 차량이 있을 때만 시드하도록 가드 처리 — 임의로 차량을 만들지 않음. 실제 Compatibility 상태를 그대로 가져와 기록) 추가.
- **관리자 "설치 영상 관리" 탭**이 30개 부품 전부 빈 링크였음 → 3개 부품(Cub 머플러/캐리어, PCX 캐리어)에 실제 유튜브 링크 등록.
- 나머지 화면(홈/로그인/부품찾아보기/커뮤니티/관리자 CRUD 탭들)은 이미 충분히 채워져 있어서 추가 보강 없음.

### 7. 최종 재검증

`./gradlew test`로 단위 테스트 + `SmokeTest`(H2, MockMvc 기반 전체 흐름) 재실행 — 시드 확장 후에도 **전부 통과**. 이어서 실제 로컬 MySQL에 `bootRun`으로 직접 붙여서 재실행해도(이미 예전 시드로 채워진 DB 위에) 새로 늘어난 제조사/부품/게시글만 정확히 추가되고 기존 회원/차량이 중복 생성되지 않는 것까지 API 호출로 확인함(DataSeeder를 idempotent find-or-create 방식으로 전면 리팩터링한 덕분).

Playwright로 부품찾아보기(즐겨찾기 별 표시/설치영상 링크), 마이페이지(즐겨찾기·최근확인 목록), 커뮤니티 목록/상세(영상 임베드 2종), 관리자 설치영상 관리 탭까지 스크린샷으로 직접 확인 완료 — 전부 정상 렌더링.

### 참고 — 시드 재실행 관련

DataSeeder는 이제 앱을 재시작할 때마다 항상 실행되지만(예전처럼 "데이터가 하나라도 있으면 전체 스킵"하지 않음), 각 항목을 이름/조합 기준으로 존재 여부를 먼저 확인하고 없을 때만 추가하는 방식이라 안전합니다. 다만 게시글(Post)은 자연어 콘텐츠라 정확한 중복 판정이 어려워서, "게시글이 하나도 없을 때만" 전체 배치로 한 번만 추가하도록 가드해뒀습니다 — 이미 게시글이 있는 DB에 이 시더를 다시 돌려도 게시글이 중복되진 않지만, 9개짜리 배치가 일부만 있는 상태에서는 나머지가 채워지지 않으니 참고해주세요.
