spring.profiles.active=local

# AI_SYNTH_MODE=mock|live. 기본은 mock이며, live로 바꾸기 전까지는 실제 API를 호출하지 않는다.
ai.synth.mode=${AI_SYNTH_MODE:mock}
pollinations.api-key=${POLLINATIONS_API_KEY:}
gemini.api-key=${GEMINI_API_KEY:}
